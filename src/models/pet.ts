export class Pet {
  constructor(
    public id: string,
    public name: string,
    public species: string,
    public carry: string,
    public weight: string,
    public date_of_birth: string
  ) {}
}
